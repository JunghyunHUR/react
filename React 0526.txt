React

useState

const [state, setState] = useState("초기값");

setState("이 안에 값을 수정")
serState((prevState) => {
	return newState;
});


useEffect

useEffect(( ) => {
	작업
},[value, val])


1. 기본 값으로 10을 받은 후 하나씩 증가하는 버튼, 하나씩 감소하는 버튼을 이용하여
  화면에 내용을 출력하는 app을 만드세요.














































